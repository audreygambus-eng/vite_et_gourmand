{% extends 'base.html.twig' %}

{% block title %}Espace employé - Vite & Gourmand{% endblock %}

{% block body %}
<h1>Espace employé</h1>
<p>Bienvenue dans votre espace</p>

    <div class="row mt-4">
        <div class="col-md-3 mb-3">
            <a href="{{ path('app_espace_employe_menus') }}" class="btn btn-outline-primary w-100">Gestion des menus</a>
        </div>
        <div class="col-md-3 mb-3">
            <a href="{{ path('app_espace_employe_plats') }}" class="btn btn-outline-primary w-100">Gestion des plats</a>
        </div>
        <div class="col-md-3 mb-3">
            <a href="{{ path('app_espace_employe_horaires') }}" class="btn btn-outline-primary w-100">Gestion des horaires</a>
        </div>
        <div class="col-md-3 mb-3">
            <a href="{{ path('app_espace_employe_commandes') }}" class="btn btn-outline-primary w-100">Gestion des commandes</a>
        </div>
        <div class="col-md-3 mb-3">
            <a href="{{ path('app_espace_employe_avis') }}" class="btn btn-outline-primary w-100">Gestion des avis</a>
        </div>
    </div>
{% endblock %}
